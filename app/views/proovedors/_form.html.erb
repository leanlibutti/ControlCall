<%= form_for(@proovedor) do |f| %>
  <% if @proovedor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proovedor.errors.count, "error") %> prohibited this proovedor from being saved:</h2>

      <ul>
      <% @proovedor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :lines %><br>
    <%= f.text_field :lines %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
