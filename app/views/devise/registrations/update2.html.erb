<div class="row"> 
  <div class="col-md-6 col-md-offset-3">

    <div class="panel panel-success">
      <div class="panel-heading">
        <h3 class="panel-title">Ingresar Sector</h3>
      </div>
      <div class="panel-body">

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
       	  <%= f.hidden_field :id %>
          <%user = User.find(:id)%>
          <form>
  			   <div class="form-group">
            	<%= f.label :sector, "Area" %></br>
            	<%= collection_select(:user :sector_id, user.area.sectors.all, :id, :name) %>
            	<% if resource.errors.messages[:area] && !resource.errors.messages[:area].empty? %>
                <%= "El area no es válido" %>
              <%end%>
          	</div>

          	<div class="form-group">
              <%= f.submit "Ingresar", class: "btn btn-success btn-block"%>
          	</div>
          </form>
        
        <%end%>
        <%= render "devise/shared/links" %>
      </div>
    </div>    
  </div>
</div>
